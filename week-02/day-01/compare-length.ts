export{};
'use strict';

let firstList: number[] = [1, 2, 3];
let secondList: number[] = [4, 5];

if (secondList.length > firstList.length) {
  console.log('The second list is longer than the first one');
} else {
  console.log('The first list is longer than the second one');
}
