'use strict';

// (two-numbers) Doing operations on x and y
function qikmafs(num1, num2) {

  return console.log(`${num1 + num2}\n${num2 - num1}\n${num1 * num2}\n${num2 / num1}\n${num2 % num1}`);
}
qikmafs(13, 22);
