'use strict';

// Doing operations on x and y
function qikmafs(num1, num2) {
  return `${num1 + num2}\n${num2 - num1}\n${num1 * num2}\n${num2 / num1}\n${num2 % num1}`;
}
console.log(qikmafs(13, 22));
